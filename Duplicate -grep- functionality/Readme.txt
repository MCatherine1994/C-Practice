Duplicate the functionality of the grep -F -f invocation; 
Take a single word as a command line argument and read lines of text from standard input, then removes all occurrences of the word from each line.
